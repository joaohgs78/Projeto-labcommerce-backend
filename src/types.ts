export type TUser = {
    id: string;
    name: string;
    email: string;
    password: number;
    createdAt: string;
  };
  
  export type TProducts = {
    id: string;
    name: string;
    price: number;
    description: string;
    imageUrl: string;
  };


  